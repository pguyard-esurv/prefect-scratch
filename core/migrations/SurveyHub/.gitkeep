# Migration files for SQL Server SurveyHub database (read-only)
# Note: This database is typically read-only, migrations may not be needed
# Files should follow the naming convention: V{version}__{description}.sql if used